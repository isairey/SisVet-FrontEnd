# Servicios compartidos

Espacio para lógica transversal al dominio (notificaciones, exportaciones, cache adapters, etc.). Cada servicio debe exponer funciones puras que interactúan con `src/api/httpClient` y son consumidas por los módulos.

